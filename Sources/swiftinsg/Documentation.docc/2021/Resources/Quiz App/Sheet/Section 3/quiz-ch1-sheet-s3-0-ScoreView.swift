import SwiftUI

struct ScoreView: View {
    var body: some View {
        Text("Hello, World!")
    }
}

struct ScoreView_Previews: PreviewProvider {
    static var previews: some View {
        ScoreView()
    }
}
