import SwiftUI

struct CatFactView: View {
    var body: some View {
        Text("Hello, World!")
    }
}

struct CatFactView_Previews: PreviewProvider {
    static var previews: some View {
        CatFactView()
    }
}
